<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.paysez.export">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MerchantLoginActivity"
            android:noHistory="true" />
        <activity android:name=".PaymentLinksActivity" />
        <activity android:name="com.paysez.library.CommonWebViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".EmailSmsActivity" />
        <activity
            android:name=".NetBanking"
            android:noHistory="true" />
        <activity android:name=".CardPaymentActivity" />
        <activity android:name=".UPI_Activity" />
        <activity android:name=".ActionSelectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>